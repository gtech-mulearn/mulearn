//A table requires minimum two children, so this is a blank component to be used as a placeholder to match the number of children required by the table component.

export const Blank = () => {
    return <></>;
};
