type Props = {}

const Settings = (props: Props) => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100vh',
        fontSize: '5rem'
      }}
    >Settings</div>
  )
}

export default Settings